server:
  port: 8080
spring:
  application:
    name: car-management-service
  datasource:
    password: postgresql
    url: jdbc:postgresql://localhost:5432/car_management
    username: postgresql
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true